package com.example.galaxia.viewmodel

/**
 * Classe base para os ViewModels do GalaxIA (camada MVVM).
 *
 * Ainda vazia de propósito: assim que a dependência do Jetpack
 * ViewModel/LiveData for adicionada ao build.gradle.kts, esta classe
 * deve passar a estender androidx.lifecycle.ViewModel e centralizar
 * comportamentos comuns (ex.: estado de loading/erro) para os
 * ViewModels concretos (ex.: ApodViewModel, FavoritesViewModel).
 */
abstract class BaseViewModel
